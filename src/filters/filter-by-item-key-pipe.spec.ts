import { FilterByItemKeyPipe } from './filter-by-item-key-pipe';

describe('FilterByItemKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByItemKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
