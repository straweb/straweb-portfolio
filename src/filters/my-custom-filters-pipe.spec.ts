import { MyCustomFiltersPipe } from './my-custom-filters-pipe';

describe('MyCustomFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new MyCustomFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
